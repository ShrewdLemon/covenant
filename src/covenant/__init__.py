"""Covenant — pytest for credit-model governance.

A model's documentation is a set of testable claims about its behaviour,
so test them. Install as ``pip install covenants``, import as ``covenant``.
"""

__version__ = "0.3.0"
